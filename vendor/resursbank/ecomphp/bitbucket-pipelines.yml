# Guides: https://confluence.atlassian.com/x/e8YWN for more examples.
# Only use spaces to indent your .yml configuration.
# -----
# You can specify a custom docker image from Docker Hub as your build environment.
# image: php:7.1.1
#          - vendor/bin/phpunit tests/curltests.php
image: php:7.1.1

pipelines:
  default:
    - step:
        caches:
          - composer
        script:
          - apt-get update && apt-get install -y unzip php-soap libxml2-dev git
          - docker-php-ext-install soap
          - docker-php-ext-enable soap
          - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
          - composer install
          - composer require phpunit/phpunit
          - vendor/bin/phpunit tests/ResursBankTest.php
